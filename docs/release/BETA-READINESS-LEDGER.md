# Auxora Windows Public Beta Readiness Ledger

This ledger is the evidence-backed release record for the first Windows-only Auxora public beta. A source check can close a code finding, but only an exact installed candidate exercised on a disposable Windows VM can close the artifact gates.

## Baseline

- Branch: `codex/fix-first-run-display-flow`
- Baseline commit: `edfa17018a495e06a7037a7cdb0603516776ec6c`
- Public repository: `SilverFuel/xeneon-widgets`
- Current public release: `v0.2.0` (`XENEON Edge Host v0.2.0 Free Public Beta`, published 2026-04-26)
- Open issues inspected: `#4 Refresh times`; no direct beta-blocker classification without further evidence.
- Initial dirty tree: 23 tracked first-run/display files, intentionally preserved.
- Integrated commits: `c749aa9` (first-run display), `aab9151` (remote/calendar security), `c1020e0` (Windows beta release pipeline), `857acc1` (privacy/recovery/rendered QA).

## Findings and gates

| ID | Finding / gate | Severity / class | Evidence | Owner | Acceptance test | Status | Validation receipt |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BETA-001 | Integrate the existing first-run display picker and readiness work without losing current changes. | High / beta blocker | `app/MainWindow.xaml:35`, `app/MainWindow.xaml.cs:196`, `app/Services/DisplayManager.cs:34`, plus the initial 23-file working-tree diff | Lead | Independent diff review; `npm run check`; `npm run audit:deps`; `git diff --check`; intentional commit | Closed in source | Product/recovery independent review passed; baseline checks passed; committed as `c749aa9` |
| BETA-002 | Prerelease versions do not follow SemVer ordering because the parser discards everything after `-` or `+`. | High / beta blocker | Original `app/Services/ReleaseService.cs:182-203` | Release/QA | Tests cover `beta.1 < beta.2 < rc.1 < stable`, numeric prerelease identifiers, and build metadata | Closed in source | 46/46 .NET tests pass after independent review correction; commit `c1020e0` |
| BETA-003 | A missing beta/nightly match falls back to the first release, crossing channels. | High / beta blocker | Original `app/Services/ReleaseService.cs:140-168` | Release/QA | Empty-match tests return unavailable for beta and nightly without returning stable/other-channel assets | Closed in source | Channel/no-fallback tests pass; commit `c1020e0` |
| BETA-004 | The beta build is still versioned `0.3.0` and defaults to the stable channel. | High / beta blocker | Original `app/XenonEdgeHost.csproj:13`, `app/Infrastructure/ConfigStore.cs:155` | Release/QA | Candidate is `0.3.0-beta.1`; beta builds default/check beta; stable builds default/check stable | Closed in source | `Test-ReleaseVersion.ps1` reports `0.3.0-beta.1`; commit `c1020e0` |
| BETA-005 | The public-beta workflow still builds and publishes the unfinished macOS scaffold. | High / beta blocker | Original `.github/workflows/release.yml:71-109` | Release/QA | Workflow contains no macOS job or macOS release dependency/assets; regression check enforces Windows-only beta | Closed in automation | Independent review confirmed Windows-only workflow; commit `c1020e0` |
| BETA-006 | Re-running an existing tag deletes its assets and uploads replacement bytes. | Critical / beta blocker | Original `.github/workflows/release.yml:124-130` | Release/QA | Existing tag/release causes a hard failure; workflow never deletes/replaces published assets | Closed in automation | Immutable workflow and fixtures pass; commit `c1020e0` |
| BETA-007 | The release path does not yet bind one candidate to an exact tag, version, commit SHA, filename, hash, and asset allowlist. | High / beta blocker | Original `.github/workflows/release.yml:18-68`, `.github/workflows/release.yml:111-132` | Release/QA | Manifest records all identifiers; workflow checks agreement and uploads only the exact five-item allowlist | Closed in automation | Manifest/lifecycle fixtures accept exact data and reject extra assets/incomplete receipts; commit `c1020e0` |
| BETA-008 | Native-host API coverage is source-pattern inspection, not a launched-host test. | High / beta blocker | Original `scripts/test-native-host-api.mjs:1-84` | Release/QA | Test launches the native host, waits for it, calls live health/config/assets and verifies origin/mutation boundaries, then terminates it | Closed in source | `npm run test:native-host-api` passed against a launched host; commit `c1020e0` |
| BETA-009 | Dashboard visual/accessibility smoke is source-pattern inspection rather than rendered-DOM geometry and interaction coverage. | High / beta blocker | Original `scripts/check-dashboard-visual-a11y.mjs:1-217` | Product/Recovery | Rendered tests cover compact, standard, ultrawide, portrait, keyboard, touch target size, focus, and accessibility with geometry assertions | Closed in source | Isolated launched host + system Edge passed 1280x400, 1440x900, 2560x720, and 800x1280 geometry, focus, activation, 32px targets, and accessible names |
| BETA-010 | Release trust UI says “Verified” when the service only found hash/signature sidecars and did not verify either. | High / beta blocker | Original `app/Services/ReleaseService.cs:249-269`, current `js/widgets/product.js` Updates renderer | Release/QA + Product | UI stays “Not verified” until downloaded bytes, checksum, and signer are actually verified; tests prevent “Verified” from availability-only state | Closed in source | UI requires `trust.trusted=true` and `trust.verificationStatus=verified`; hash/signature sidecar presence is displayed separately and never creates a trusted state. Focused and full checks pass. |
| BETA-011 | The exact installer has not passed the required disposable-VM install-closed, deliberate launch, live `/api/health`, process restart, no-autostart reboot, injected failed-upgrade rollback, successful upgrade, repair, uninstall, and remove-all-data cycle. | Critical / user-only operational gate | `docs/release/CLEAN-INSTALL-TEST.md`; source checks and an in-place install cannot prove the complete disposable lifecycle | Lead + user | Completed schema-3 VM evidence names the exact installer and SHA-256 and records every lifecycle result, including restored files/settings/shortcuts/uninstall metadata and fail-safe startup removal after an injected failure | Blocked on disposable Windows VM and approval to run its destructive lifecycle | Earlier revision `20260722-56` installed in place with live health and exact binary identity, but no current-candidate schema-3 lifecycle receipt or injected failed-upgrade rollback evidence exists |
| BETA-012 | The pre-existing local installers predate the integrated beta work and cannot be the release candidate. | High / beta blocker | Historical `app/dist/Auxora-Setup-0.3.0-20260714-2313.exe` and the original dirty source state | Lead | Build from the final clean commit; verify the exact workflow artifact, `NotSigned` status, five-asset manifest, checksum, and non-installing gauntlet | Pending exact workflow candidate | No authoritative candidate for the current commit has been built and verified. Installer identity, `NotSigned` status, manifest binding, and all exact-candidate receipts remain pending. |
| BETA-013 | “Reset all app data” clears config and protected secrets but leaves recent-app history, logs, retained PresentMon captures, WebView data, and one dashboard storage key. | High / beta blocker | Original `app/Infrastructure/ConfigStore.cs:59-73`, `app/Services/LauncherService.cs:44-58`, `app/Services/GamePerformanceService.cs:140-146`, `app/MainWindow.xaml.cs:69-72`, `js/dashboard.js:2169-2175`, `js/dashboard.js:2343-2348` | Product/Recovery | Seed every retained store, call the real reset API, and assert complete deletion or a visible failure | Closed in source | Seeded-store reset and visible-failure tests pass; launched initialized WebView returned `webview-data: cleared`; dispatcher access independently reviewed |
| BETA-014 | Foreground-app tracking starts automatically, polls every three seconds, persists executable history, and has no user toggle or adequate disclosure. | High / beta blocker | Original `app/Services/LauncherService.cs:14-18`, `app/Services/LauncherService.cs:44-58`, `app/Services/LauncherService.cs:342-406`, `app/Models/AppConfig.cs:69-118`, `js/widgets/product.js:1266-1283` | Product/Recovery | Plain disclosure plus toggle; polling-off test proves no capture/persistence; reset clears retained history | Closed in source | Default-off/on/purge tests and in-flight capture-vs-opt-out race test pass; disclosure and toggle are present in Setup and Privacy |
| BETA-015 | Installed Recovery exposes developer packaging controls instead of customer recovery actions. | High / beta blocker | Original `js/dashboard.js:1973-1979`, `js/widgets/product.js:1178-1228`; customer actions existed separately at `app/TrayIcon.cs:153-170` and `app/installer/Install-XenonEdgeHost.ps1:265-290` | Product/Recovery | Installed panel provides Retry, Repair, Restart in Safe Mode, Open Logs, Quit; no repo path/build/signing UI | Closed in source | Customer recovery API/UI and safe fixed-script process arguments pass focused tests; unpackaged-only actions report unavailable |
| BETA-016 | Phone Remote put a bearer token in the URL and rendered user-controlled Scene names through `innerHTML` and inline JavaScript. | Critical / beta blocker | Original implementation at `app/Services/RemoteSessionService.cs:53-57`, `app/Services/RemoteSessionService.cs:113-166`; Scene names were only trimmed/length-limited at `app/Services/SceneDefaults.cs:72-76` | Security/Privacy + Product | Beta snapshot/UI report unavailable; start cannot bind; no token URL, QR, generated HTML, or LAN listener path remains | Closed in source | Service and UI truthfully report unavailable; no start/token/QR action remains; full .NET and dashboard contract checks pass; backend commit `aab9151` |
| BETA-017 | Calendar accepted HTTP and validated only the literal hostname before a redirect-following shared-client request; the feed URL was stored in plaintext. | Critical / beta blocker | Original implementation at `app/Infrastructure/NetworkEndpointGuard.cs:38-57`, `app/Services/CalendarService.cs:74-90`, `app/BridgeManager.cs:73-81`, `app/Infrastructure/ConfigStore.cs:278-342` | Security/Privacy + Product | Protected migration; HTTPS-only; all resolved addresses and redirect hops revalidated; automatic redirects disabled; reset clears secret; connection-boundary rebinding test | Closed in source | Direct connection-boundary rebinding test rejects loopback before connector; production handler guard asserted; full tests pass; backend commit `aab9151` |
| BETA-018 | The release-readiness gate rejects the intended `0.3.0-beta.1` version even when the gauntlet explicitly selects the unsigned free-beta path. | High / beta blocker | Original `scripts/assert-release-ready.ps1:73-76`; exact-candidate gauntlet failed at the version gate | Release/QA + Lead | Exclusive strict-ASCII SemVer modes: unsigned-beta accepts only `beta.N`; default/signed accepts only stable; conflicting flags, leading zeros, and Unicode digits reject | Closed in source | Executable 11-case mode matrix and gauntlet regression guard pass; independent review closed additive-mode and Unicode-digit findings |
| BETA-019 | The public beta feed is older than the installed candidate but the native release response still exposes its installer, macOS, and sidecar download locations. | High / beta blocker | Installed revision `20260719-14` `/api/releases/latest?channel=beta` returned `versionRelation=older` together with `v0.2.0` download URLs | Release/QA + Product | Native API sets `downloadAllowed=false`, blanks top-level installer/macOS URLs, and redacts all asset/sidecar locations unless the feed is provably newer; UI separately requires the same permission | Closed locally | Focused release suite passes 10/10; static guard enforces API and UI defenses; revision `20260719-15` installer exited 0 and its live older-feed response exposed no installer, macOS, asset, hash, or signature locations; complete sequential `npm run check` passed |
| BETA-020 | The beta build can display and persist the Stable channel while native health reports the assembly version `0.3.0.0`, contradicting the actual `0.3.0-beta.1` candidate. | High / beta blocker | Installed revision `20260719-13` Updates panel and `/api/config`; health version before the shared build-identity fix | Release/QA + Product | Health, support, release service, config normalization, and Updates use one semantic build identity; a beta build shows Beta and cannot offer Stable | Closed locally | Shared `AppBuildIdentity` tests pass 7/7; revision `20260719-14` installed health reported `0.3.0-beta.1`, config reported `releaseChannel=beta`, and rendered UI omitted Stable |
| BETA-021 | The receipt-bound publication workflow and manual free-beta helper require lifecycle evidence but can publish or recommend upload without evidence from a physical Frigate server and real camera. | Critical / beta blocker | Original `.github/workflows/release.yml` accepted only `lifecycle_receipt_base64`; original `scripts/prepare-free-beta-release.ps1` printed a two-file upload list after local source/artifact checks | Release/QA + Camera Detection | Publication requires a secret-free Frigate receipt bound to the exact manifest filename/hash/version/commit; partial evidence and fixture-only receipts fail closed; receipts are never public assets | Closed in automation | Workflow input is mandatory; local receipt-bound gauntlet requires all three evidence paths; manual preparation rejects missing receipts; valid and five invalid receipt fixtures pass; complete sequential `npm run check` passes |
| BETA-022 | The exact proposed release candidate has not been qualified against a physical Frigate server and real camera on the target LAN. | Critical / user-only operational gate | Mock/native rendering proves the connector contract but cannot prove the target server, camera, TLS trust, restricted role, filtering, token expiry, or physical disconnect/reconnect behavior | Lead + user | Complete `docs/release/FRIGATE-CERTIFICATION.md`; verify the secret-free schema-1 receipt against the exact release manifest with `scripts/Test-FrigateQualificationReceipt.ps1` | Blocked on physical Frigate/camera environment | Revision `20260722-56` passed authenticated Camera Detection live/loss/cache/reconnect rendering plus physical-receipt rejection fixtures, but the installed host remains unconfigured and no non-fixture physical receipt exists, so publication remains closed |
| BETA-023 | Display certification is prose-only, while publication can proceed without exact-candidate evidence for primary exclusion, taskbar hiding, hot-plug/role switching, scaling, touch, keyboard, and screen-reader behavior. | Critical / beta blocker | Original `docs/release/DISPLAY-CERTIFICATION.md` had a pending matrix but no verifier, receipt schema, fixture, or publication input; lifecycle evidence covers only basic non-primary startup | Release/QA + Product | A privacy-safe display receipt binds the exact manifest and requires physical Windows/touch hardware plus all placement, recovery, scaling, input, accessibility, and flicker checks | Closed in automation | Verifier and six rejection fixtures pass; local gauntlet, manual helper, and GitHub publication job require the display receipt; complete sequential `npm run check` passes |
| BETA-024 | The exact proposed release candidate has not completed the physical companion-display certification matrix. | Critical / user-only operational gate | Automated HWND sampling and rendered layout tests cannot prove physical unplug/replug, Windows role changes, taskbar behavior, scaling readability, touch gestures, or screen-reader output | Lead + user | Complete `docs/release/DISPLAY-CERTIFICATION.md` and verify its schema-1 receipt against the exact release manifest | Blocked on physical display/touch sessions | Revision `20260722-56` is installed on the physical XENEON EDGE at native 2560×720/60 Hz; live health and startup logs identify it as non-primary at `(619,2160)`, show taskbar-free tool-window policy, and show successful dashboard navigation. The complete gate sampled 162,557 placements; the prior revision-55 direct capture proved edge-to-edge coverage with no taskbar/error overlay, and the earlier move/cancel pass proved constrained mobility and sticky restoration. Automated reduced-motion and all-23-surface Windows forced-colors coverage now pass, but no complete non-fixture receipt exists for unplug/replug, primary-role switching, the full physical scaling matrix, touch/long-press, screen-reader, or human-observed flicker. |
| BETA-025 | Installed Recovery remains on `Checking`, repeated Layout and Mode controls have ambiguous accessible names, and Updates overstates supported channels/automatic behavior. | High / beta blocker | Live installed revision-15 browser audit across Recovery, Layout Editor, Modes, and Updates | Product/Recovery + Release/QA | Recovery publishes a terminal state; every repeated action has a unique spoken name; beta Updates exposes only supported channels and makes no automatic-install claim; source/rendered/live installed checks pass | Closed locally | Revision `20260719-16` complete `npm run check` passed; exact installer hash verified; installed health reported revision 16; live 1280x720 audit showed Recovery Ready, 55 unique Layout names, eight unique Mode names, Beta/Nightly only, no dashboard error/overflow, and no console warnings/errors |
| BETA-026 | Camera Detection relies on Frigate to honor camera/time query filters, and the localhost snapshot proxy accepts any syntactically valid event ID instead of binding access to current filtered results. | Critical / beta blocker | Revision-16 `FrigateService.GetSnapshotCoreAsync` parsed all returned events; `GetEventSnapshotAsync` validated ID syntax only before proxying upstream | Camera Detection + Security/Privacy | Locally enforce selected camera, one-hour lookback, bounded future skew, explicit snapshot availability, usable authentication token, and current-result snapshot authorization; return 404 without an upstream snapshot request for unavailable IDs | Closed locally | Revision `20260720-17` focused Frigate suite passed 20/20 and complete `npm run check` passed 131/131; installed snapshot requests returned 404 while unconfigured/invalid; live Diagnostics rendered the protected Camera Detection form with no dashboard or console errors |
| BETA-027 | Streaming reports `Available` and accepts an arbitrary WebSocket host even though the beta implements only a local OBS reachability and layout preview. | High / beta blocker | Revision-17 Streaming product surface, rendered semantics, and installed browser interaction | Product/Streaming + Security/Privacy | Label the feature `Preview`; state that it sends no OBS commands; accept only credential-free loopback `ws:`/`wss:` endpoints; save and probe the exact submitted endpoint in one explicit action | Closed locally | Revision `20260720-19` complete `npm run check` passed 131/131; installed 1280x720 UI showed Preview, rejected `ws://example.com:4455` as `Local address required`, restored the saved loopback endpoint, and had no dashboard failure or overflow |
| BETA-028 | Privacy & Backup claims portable presentation and Modes but omits layout/Mode state, validates client-only fields after native restore, and lets a stale diagnostics refresh overwrite a newer backup result. | High / beta blocker | Revision-19 `mountPrivacyWidget`, native portable backup response, and rendered interaction audit | Product/Privacy + Release/QA | Export the credential-free portable presentation allowlist; validate it before the native POST; preserve native JSON value types; prevent stale async work or a state refresh from replacing the newest action result | Closed locally | Revision `20260720-20` complete `npm run check` passed 131/131; rendered export/invalid/valid round trip passed; installed malformed restore stayed rejected with zero `/api/config/backup` POSTs, no dashboard failure/overflow, and exact companion placement |
| BETA-029 | Media metadata and audio application labels are implemented as default-off native privacy controls, but no shipped UI lets a user enable them or explains why Audio & Media remains generic. | High / beta blocker | Revision-20 native `MediaMetadataVisible` and `AudioSessionLabelsVisible` configuration and installed Privacy/Audio surfaces | Product/Privacy + Audio & Media | Keep both defaults off; add separate plain-language opt-ins; persist through the native API and portable backup; prove enable/disable behavior and return to private state | Closed locally | Revision `20260720-21` complete `npm run check` passed 132/132; rendered UI enabled and disabled both native settings; installed 1280x720 UI and `/api/config` confirmed both transitions, private final state, no dashboard failure/overflow, and exact companion placement |
| BETA-030 | Camera Detection is supported by the native host but disappears from Apps & Controls until configured, leaving setup buried behind the secondary Settings drawer, Diagnostics, and optional extras. A hydrated Camera deep link can also show the panel while incorrectly highlighting Settings. | High / beta blocker | Installed revision-21 unconfigured Apps & Controls, Diagnostics, and direct `?widget=frigate` route | Product/Camera Detection | Show supported Camera Detection before configuration with an honest Setup state; link directly to Diagnostics; keep its copy truthful; ensure hydrated deep links highlight Apps & Controls; prove source, rendered, and installed behavior | Closed locally | Revision `20260720-22` complete `npm run check` passed 132/132; rendered and installed 1280x720 audits showed Camera Detection Setup in Apps & Controls, direct Diagnostics handoff, correct navigation ownership, no dashboard failure/overflow, and exact companion placement |
| BETA-031 | `Set up Camera Detection` switches to Diagnostics but leaves Optional extras collapsed, so the promised Camera form is absent and the user must still discover and press `Show extras`. | High / beta blocker | Installed revision-22 Camera Detection-to-Diagnostics handoff at 1280x720 | Product/Camera Detection + Accessibility | Carry a targeted setup request into the mounted Diagnostics renderer; expand optional setup; scroll Camera Detection into view; focus the Frigate address after async refresh; preserve navigation, overflow, and runtime safety | Closed locally | Revision `20260720-23` complete `npm run check` passed 132/132; rendered and installed audits proved the visible Frigate form, `baseUrl` focus, expanded extras, contained geometry, no dashboard failure/overflow, and exact companion placement |
| BETA-032 | Display Controls enumerates every logical monitor, allowing DDC/CI discovery and brightness, contrast, input, or power commands to target the Windows primary display. | Critical / beta blocker | Revision-23 live `/api/displays/controls` returned both the primary logical monitor and the companion monitor; GET and POST shared the same unfiltered enumeration | Product/Display + Safety | Classify every logical monitor with `GetMonitorInfo`; include only confirmed non-primary displays; fail closed when classification fails; use the same filtered set for reads and writes; expose and test the companion-only contract | Closed locally | Revision `20260720-24` source and 136/136 native tests passed; launched-host API reported `scope=companion-only` and `primaryExcluded=true`; exact installed API returned one controllable companion instead of the previous two physical-monitor entries and explicitly stated that primary is excluded |
| BETA-033 | Theme Studio replaces its live DOM while a setting is focused, allowing the browser's focus/blur cleanup to collide with `innerHTML` removal and collapse the dashboard into the reported runtime-error overlay. | High / beta blocker | User-provided installed screenshot: `Failed to set the 'innerHTML' property ... node to be removed is no longer a child ... moved in a 'blur' event handler?` over Theme Studio | Product/UI + Accessibility | Patch the existing DOM while preserving the active control and selection; tolerate focus handlers moving nodes during reconciliation; rendered regression changes a focused Theme control and rejects runtime/fatal errors | Closed locally | Revision `20260720-24` focused rendered regression, full theme matrix, 24-cycle installed Theme Studio stress audit, and 1280x720 live audit passed with connected controls, no dashboard failure, and no overflow |
| BETA-034 | Revision 24 fixed Theme Studio, but other periodically refreshed panels and dynamic dashboard controls still replaced live DOM. Revision 25 stopped the crash class, yet a real Refresh click moved focus and cleared unsaved Privacy and Camera Detection drafts. | High / beta blocker | Source-wide interactive-renderer inventory plus live installed revision-25 Privacy Refresh: connected textarea reset from `draft backup text` to empty after focus moved to Refresh | Product/UI + Camera Detection + Accessibility | Use multi-root stable reconciliation for every live redraw and teardown; key action/form controls; preserve dirty input, textarea, checkbox, radio, and select state when focus moves; keep busy actions focusable and reject duplicate requests; reproduce real focus movement in rendered and installed audits | Closed locally | Revision `20260720-26` complete check passed 136/136; installed Privacy retained `revision 26 draft` with Refresh focused, Camera Detection retained `http://camera-draft.local:5000` with Refresh focused, Streaming retained its focused draft, and all three had connected controls, no overflow, and no dashboard failure |
| BETA-035 | A late asynchronous response from a panel that was already closed could still redraw the shared viewer over the newly selected panel. Camera Detection also retained old events after a refresh failure without labeling them cached, allowed overlapping refreshes, polled every 10 seconds, and could say `Fresh Frigate sample` beside `Not updated`. | High / beta blocker | Source audit of the shared inline viewer and Camera refresh paths; revision-27 installed audit rejected the contradictory `Not updated` / `Fresh Frigate sample` state | Product/UI + Camera Detection + Accessibility | Give every mount a disposable isolated root; preserve semantic form identity through reordered controls; serialize refreshes; use a 30-second poll; label connection-loss data cached; never claim Fresh without a sample; prove teardown, failure, and reconnect behavior in rendered and installed audits | Closed locally | Revision `20260720-28` complete check passed 136/136; rendered tests proved reordered multi-control drafts, serialized refresh, stale-mount isolation, Camera connection-loss cache and reconnect; installed rapid panel switching ended on exactly one System Monitor mount, and unconfigured Camera showed `Not updated` / `Waiting for setup` without Fresh |
| BETA-036 | The global dashboard exposed two controls named `Settings`, private Audio sessions repeated indistinguishable `Mute` and volume names, and revision 29's paused Camera timer was bypassed by the dashboard health refresh, causing an unconfigured Frigate request every 30 seconds. | High / beta blocker | Installed revision-28 accessibility audit plus revision-29 native log evidence showing a second `/api/frigate` request exactly at the 30-second dashboard refresh | Product/UI + Camera Detection + Accessibility | Use distinct visible and spoken Panel options wording; give private Audio rows stable unique accessible names without exposing app identity; make the Camera controller's public automatic refresh a no-op while unconfigured; keep the visible manual Refresh immediate; prove quiet background behavior and manual retry in the exact installed candidate | Closed locally | Revision `20260720-30` complete check passed 136/136; installed UI had no duplicate or unnamed visible controls, private Audio exposed unique `Audio App 1/2` names, Camera logged one request after 34 seconds and exactly two only after a real manual Refresh click |
| BETA-037 | System Monitor again enumerated every running process and exposed the top five application names, PIDs, CPU, and memory even though the product changelog said Top Processes had been removed. | High / beta blocker | Installed revision-30 System Monitor plus live `/api/system` response containing `topProcesses`, including local application identity and process IDs | Product/UI + Privacy + Native telemetry | Remove process enumeration and the `topProcesses` API schema from System telemetry; remove application and PID rendering from inline and standalone System Monitor; retain overall CPU/GPU/RAM telemetry plus an explicit Task Manager handoff; add source, rendered, and launched-host guards | Closed locally | Revision `20260720-31` complete check passed 136/136; rendered and live-host tests reject `topProcesses`; the exact installed API omitted the field and the installed 2560x720 UI showed `Privacy-safe telemetry` with no application names or PIDs |
| BETA-038 | Network Monitor chose the highest advertised link speed, causing a 10 Gbps Hyper-V switch with no gateway or DNS to replace Windows' routed 2.5 Gbps physical Ethernet path. The resulting local-router ping was also labeled as general game readiness, and custom accents could make the success values unreadable. | High / beta blocker | Revision-31 installed `/api/network` named `vEthernet (Default Switch)` at 10 Gbps while the live Windows default route used interface 12 `Ethernet`, Realtek PCIe 2.5GbE, gateway `192.168.1.254`; revision-32 installed screen then exposed low-contrast Network success values | Product/UI + Native telemetry + Accessibility | Select the interface returned by Windows routing, fall back to an IPv4 gateway rather than raw speed, sample throughput only from that adapter, reset deltas on route changes, label gateway probes as local-link readiness, and use semantic success text independent of a custom accent; cover route, VPN, fallback, no-address, and dark-accent cases | Closed locally | Revision `20260720-33` complete check passed 140/140 and the dark-accent rendered regression; exact installed API matched Windows' `Ethernet`/Realtek route at 2.5 Gbps with IP, gateway, and DNS; the installed 2560x720 screen showed readable `Local link ready`, `Router`, and path values while remaining exact on non-primary `DISPLAY2` |
| BETA-039 | Home still described a gateway-only probe as general network responsiveness; destructive controls looked pre-confirmed and never disarmed; Display Controls briefly showed a false empty result; Modes advertised unused brightness values; and Updates exposed an unusable automatic-rollback claim. | High / beta blocker | Installed revision-33 audit across Home, Quick Actions, System Shortcuts, Display Controls, Modes, Updates, and the rollback API | Product/UI + Recovery + Release/QA | Name the actual Home probe; make destructive actions explicit two-step controls with a short expiry; show a truthful DDC/CI loading state; display only applied Mode values; fail closed to manual rollback; cover source, rendered, native API, install, and live companion-screen behavior | Closed locally | Revision `20260720-34` complete check passed 140/140; rendered tests proved both confirmation paths expire after eight seconds; native rollback reports unavailable/manual-only; installed Home, Modes, Updates, Camera Detection, and Display Controls showed truthful terminal states; the exact installed 2560x720 topmost window covered non-primary `DISPLAY2` with no taskbar visible |
| BETA-040 | A recovered first WebView navigation left a delayed retry armed, forcing a second full dashboard load about two seconds after successful startup; periodic Home refreshes and unchanged inline widgets also performed avoidable redraw work; obsolete automatic-rollback fields remained persisted and exposed internally after the visible action was removed. | High / beta blocker | Installed revision-34 startup log showed `Dashboard loaded successfully` followed by a second complete dashboard/asset load at exactly +2 seconds; source/runtime audit found eager Home loading redraws, unconditional DOM reconciliation, and stale rollback configuration/API fields | Product/UI + Native host + Recovery + Release/QA | Cancel a scheduled retry after navigation recovery; do no DOM work for identical rendered HTML or unchanged media state; retain the prior Home sample during background refresh; migrate away obsolete rollback metadata and fail closed to manual-only recovery; cover source, rendered mutation, configuration migration, launched-host API, exact install, and timed installed-startup behavior | Closed locally | Revision `20260720-35` complete check passed 141/141; rendered regression observed zero mutations for identical HTML; installed timed startup produced one `Dashboard loaded successfully`, no second load after eight seconds, and zero warning/error lines; live config and rollback capability omitted all obsolete fields; exact 2560x720 window remained on non-primary `DISPLAY2` |
| BETA-041 | A late asynchronous status/error callback could take ownership of the next panel, redraw its selector during a tap, or surface a stale failure; the first startup-retry fix could also accept a successful blank/intermediate WebView completion as the dashboard and leave the native companion window white. | High / beta blocker | Rendered reproduction changed Network to a stale Display Controls status after navigation; installed revision 36 logged `Dashboard loaded successfully` but made no embedded dashboard API requests and captured as a white 2560x720 window | Product/UI + Native host + Release/QA | Bind callbacks to the originating widget generation; update background selector status without rebuilding the selector; accept only the tracked dashboard navigation as loaded; launched-host proof must observe embedded page requests; cover late status/error, selector node/click stability, installed startup, and native rendering | Closed locally | Revision `20260720-37` complete check passed 141/141; rendered tests ignored late status/error callbacks and preserved selector nodes/clicks; strengthened native validation observed the embedded page request `/api/config`; exact installed startup produced one dashboard-success line, two embedded config requests, and zero warning/error/non-success lines; native capture showed the rendered Diagnostics UI at 2560x720 on the companion display with no white screen, taskbar, or error overlay |
| BETA-042 | Concurrent expired Camera Detection requests could each renew the same Frigate session; rejected setup feedback appeared only in the off-screen Diagnostics toolbar; a duplicate busy form submission could fall through to browser navigation; Calendar referenced a missing fallback; and obsolete standalone widget pages were still packaged. | High / beta blocker | A new concurrent native test reproduced three logins instead of two; installed-width browser testing placed the local/private-address rejection outside the setup form; source audit found the default form action before the busy guard, `/widgets/calendar-upcoming.html`, and a wildcard embedded-resource rule shipping unreachable legacy pages | Product/UI + Camera Detection + Native host + Release/QA | Share one authentication renewal by generation; announce saving/success/rejection beside the Camera form; cancel every submit before the busy guard; distinguish test failure from post-test status-refresh failure; ship exactly the standalone fallbacks referenced by the dashboard; cover duplicate submission, local/private rejection, concurrency, asset allowlisting, and live fallback serving | Closed locally | Revision `20260720-38` complete check passed 142/142; rendered tests proved local form feedback and cancellation of both initial and duplicate submits; concurrent expired requests shared one renewal; live host served Calendar fallback and rejected an obsolete page; exact installer verification and installed launch/restart/Repair smoke passed; native placement recorded 132,652 samples with no primary-display intersection |
| BETA-043 | Native browser tests could mutate the real Auxora profile; primary navigation clipped at a supported size; Theme Studio duplicated its Theme selector; custom Modes had no complete edit/delete lifecycle; removing or resetting Camera Detection left credentials and cached detections in memory; and slow telemetry plus clipboard enumeration delayed routine health/startup checks. | High / beta blocker | The rendered suite created a real `Work Copy` Mode under `%APPDATA%`; 1280x720 measurements showed clipped primary labels; UI inspection found three Theme cards plus a Theme dropdown and no custom-Mode editor; service inspection found no in-memory Frigate purge; consecutive launched-host suites reproduced health timeouts and canceled-request errors | Product/UI + Camera Detection + Native host + Privacy + Release/QA | Require explicit fail-closed test roots and prove their config/log locations; render all primary labels without clipping; expose one Theme selector; support custom Mode save and expiring two-step delete; purge Frigate token/cache/receipt state with in-flight generation protection; queue slow telemetry; keep health checks out of clipboard contents; cover both companion-present and safe companion-absent Recovery states | Closed locally | Revision `20260720-39` complete check passed 147/147 with seven rendered geometries, 15 Theme captures, 100/100 cold boots, custom Mode lifecycle, Camera state clearing, and truthful no-companion Recovery; dependency audits found zero known vulnerabilities; exact installer verification and installed launch/restart/Repair passed; 3,650 native placement samples found no visible Auxora window on the 2560x720 primary-only topology |
| BETA-044 | The remaining global settings renderer could still replace a focused live settings body with `innerHTML`, reproducing the browser's node-removal/blur crash; non-touch scrolling could swallow the next click; an opaque Diagnostics card permanently occupied the lower-left rail; unconfigured Camera Detection rendered fake zero metrics and an empty snapshot; and clearing Camera state waited behind an in-flight login. | High / beta blocker | User-provided `Dashboard runtime error` screenshot with the browser's `innerHTML`/blur node-removal exception; installed revision-39 rail measurements and first-click reproduction; source lifecycle audit of `ClearSensitiveState` and the authentication semaphore | Product/UI + Camera Detection + Accessibility + Privacy + Release/QA | Route settings redraws through stable DOM reconciliation with re-entrant focus protection; suppress post-scroll clicks only for active touch gestures; remove the duplicate rail card and expose truthful concise status; render one unconfigured Camera setup state; cancel old credential-bearing requests without blocking configuration changes; cover source, rendered, native, install, and primary-only placement behavior | Closed locally | Revision `20260720-40` complete check passed 148/148 with seven rendered geometries, 15 Theme captures, the focus/blur and non-touch first-click regressions, 100/100 cold boots, Camera setup/live/loss/reconnect and nonblocking in-flight cancellation; dependency audits found zero known vulnerabilities; exact installer verification and installed launch/restart/Repair passed; 3,129 placement samples found no visible primary-display window |
| BETA-045 | The renderer fallback exposed internal `Inline migration` / `In Progress` wording, the canonical renderer inventory omitted Camera Detection, unmatched native feature routes claimed they were `not implemented yet`, and a newly published high-severity `brace-expansion` advisory affected six Electron packaging dependencies. | High / beta blocker | Revision-40 source-wide UI/feature fallback audit plus live `npm run audit:deps` advisory GHSA-3jxr-9vmj-r5cp | Product/UI + Camera Detection + Release/QA + Packaging security | Show a stable user-facing unavailable-panel recovery state; require a renderer for every shipped surface including Camera Detection; make unmatched routes report endpoint unavailability; update only the affected transitive lockfile releases; rerun complete source, rendered, native, audit, artifact, install, and placement evidence | Closed locally | Revision `20260720-41` complete check passed 148/148 with seven rendered geometries, 15 Theme captures, 100/100 cold boots, Camera live/loss/reconnect coverage, and 3,995 primary-only placement samples; root npm, Electron npm, and NuGet audits found zero known vulnerabilities; exact installer verification and installed launch/restart/Repair passed; installed UI loaded 24 revision-41 assets with no failure, runtime error, or horizontal overflow |
| BETA-046 | The installed dashboard could still surface the reported `innerHTML`/blur fatal overlay, Theme Studio's Custom accent handler referenced render-local state and could leave its color control disabled, Diagnostics changed names before onboarding, and raw provisioning snapshots exposed local launcher paths and browser arguments through health/config/action APIs. | High / beta blocker | User-provided revision-41 fatal-overlay screenshot; focused Theme change/blur reproduction; source and live JSON audit of `setup.provisioning`, `config.provisioning`, and `/api/provisioning` | Product/UI + Privacy + Accessibility + Release/QA | Remove interactive `innerHTML` setters; defer Theme reconciliation while a form control owns focus; make Custom accent update immediately; keep Diagnostics naming stable; expose summary-only health/config provisioning and identity-only launcher review suggestions; cover source, rendered, native, install, and live installed behavior | Closed locally | Revision `20260720-42` complete check passed 149/149 with the focused Theme change/blur and Custom accent regression, seven geometries, 15 Theme captures, 100/100 cold boots, Camera live/loss/reconnect, and 3,422 primary-only placement samples; dependency audits were clean; exact installer verification, in-place install, restart, and Repair passed; installed APIs omitted private provisioning fields and installed Theme/Diagnostics UI had no fatal state, console errors, or horizontal overflow |
| BETA-047 | Background Scene evaluation relied only on the native fetch wrapper to inject its session token, allowing periodic dashboard mutations to receive `403` responses and create a recurring rejected-request log loop. | High / reliability blocker | Installed revision-42 host log recorded `Rejected POST request without a valid session token` for `/api/scenes/evaluate` approximately every 25 seconds | Product/UI + Native API + Release/QA | Send the session token explicitly for background mutations; require the behavior in source gates; rebuild and install the exact candidate; observe multiple live evaluation cycles with `200` responses and no warnings/errors | Closed locally | Revision `20260720-43` complete check passed 149/149, seven geometries, 15 Theme captures, Camera lifecycle, 100/100 cold boots, and 4,000 primary-only placement samples; the exact installed candidate then completed four `/api/scenes/evaluate` requests with status `200` over 93.6 seconds with zero new warnings/errors while remaining windowless on the primary-only topology |
| BETA-048 | Diagnostics displayed a disabled primary-looking `Auto ready` button while the page truthfully reported `Needs Setup`, and repeated the Diagnostics name in a nested `Auto setup and diagnostics` heading. | Medium / UI clarity | Installed revision-43 browser audit at 1280x720 showed the contradictory inert control and three stacked Diagnostics labels on the companion-oriented surface | Product/UI + Accessibility + Release/QA | Render setup completion only when it is actionable; use one clear nested readiness heading; cover source and rendered semantics; rebuild, install, and audit the exact candidate | Closed locally | Revision `20260720-44` complete check passed 149/149 with seven geometries, 15 Theme captures, 100/100 cold boots, Camera lifecycle, and 3,401 primary-only placement samples; installed Diagnostics rendered `System readiness`, no inert completion control, no fatal state, no overflow, and no console messages; nine live Scene evaluations returned `200` with zero host warnings/errors |
| BETA-049 | Auxora could remain hidden forever when a companion became usable after the 30-second startup recovery window; the locked fullscreen surface had no deliberate safe move mode; Windows drove the XENEON EDGE at 1280×1024/75 Hz while Diagnostics called it ready; the companion taskbar overlaid 48 pixels after the mode change; and one stuck Windows media query produced repeating timeout warnings. | High / Display placement and runtime stability | Live revision-44 audit found two active displays and one companion while every Auxora HWND remained hidden; a later live capture showed an 853×683 logical layout backed by the wrong 1280×1024 mode, then a taskbar overlay after switching to the supported 2560×720/60 Hz mode; host logs repeated `Windows media session refresh timed out.` | Native host + Product/UI + Release/QA | Retry companion recovery until success; add a tray-owned move mode constrained to non-primary displays that returns to sticky fullscreen placement; identify `CRXED00`, reject a non-native XENEON mode in readiness, and reassert topmost/taskbar-free placement after Windows settles; cancel timed-out shared media reads and suppress duplicate reports; build, install, and exercise the exact candidate | Closed locally | Revision `20260721-46` complete check passed 151/151 with seven geometries, 15 Theme captures, 100/100 cold boots, and 173,885 live HWND samples; the exact installed candidate reported XENEON EDGE at 2560×720/60 Hz, entered constrained 1120×688 move mode, restored exact 2560×720 locked placement after a four-second settle with no taskbar, and completed 200 media requests with zero failures or new warnings/errors |
| BETA-050 | Windows exposes the connected XENEON EDGE hardware ID `CRXED00` with the generic friendly name `Generic PnP Monitor`, so Auxora recognized the device for native-mode safety but still displayed the misleading generic name in Mode display assignments and diagnostics data. | Medium / UI clarity and hardware identity | Installed revision-46 Modes audit at 2560×720 showed `Generic PnP Monitor (2560x720)` while the same health snapshot identified `CRXED00` and `containsXeneonName=true` | Product/UI + Native display + Release/QA | Normalize only the known `CRXED00` generic label to `XENEON EDGE`; preserve a specific Windows-provided friendly name and the hardware-backed stable ID; cover both cases in native tests; build, install, and verify the exact candidate | Closed locally | Revision `20260721-47` complete check passed 152/152 with seven geometries, 15 Theme captures, 100/100 cold boots, and 78,858 live HWND placement samples; exact installed health and the Modes display-assignment UI now report `XENEON EDGE (2560x720)`, while live bounds remain `(619,2160)-(3179,2880)` and Auxora remains topmost ahead of the secondary taskbar |
| BETA-051 | Diagnostics required two taps to reset all local data, but the first tap armed `Confirm reset` indefinitely. An unrelated in-flight refresh could also replace the warning with `Ready`, leaving a destructive action armed without its caution copy. | High / destructive-action safety and UI status ownership | Installed revision-47 audit: after one `Reset local data` tap, the `Confirm reset` state and `Tap again` message remained after nine seconds; rendered reproduction then showed a later click issuing one real reset inside the isolated test profile | Product/UI + Privacy + Recovery + Release/QA | Expire the confirmation after eight seconds; cancel it when another Diagnostics action takes ownership; dispose the timer when the panel closes; prevent background refresh completion from overwriting the active warning; prove expiry, cancellation, re-arming, zero native reset calls, and no runtime failure | Closed locally | Revision `20260721-48` complete check passed 152/152 with seven geometries, 15 Theme captures, 100/100 cold boots, and 75,853 live HWND placement samples; the exact installed candidate displayed `Tap again within 8 seconds`, returned to `Reset local data` with `Reset cancelled`, re-armed and cancelled safely, logged zero `/api/config/reset` requests, preserved onboarding/configuration, and remained exact and taskbar-free on the XENEON EDGE |
| BETA-052 | Touch Lock changed the label to `Unlock Touch` but did not actually block the interface; Modes, Quick controls, Diagnostics, and destructive controls remained actionable while the product claimed touch was locked. | High / touch safety and user trust | Exact installed revision-48 reproduction: Touch Lock was visibly enabled, then selecting Modes changed the active surface while the lock remained shown | Product/UI + Accessibility + Release/QA | Place every interactive surface except Unlock Touch behind a transparent full-window input guard; mark background regions inert; block pointer, touch, wheel, and keyboard activation in capture; close Quick controls on lock; persist the lock and restore deliberate focus on unlock; prove protected actions have no effect | Closed locally | Revision `20260721-49` complete check passed 152/152 with seven geometries, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect coverage, and 80,991 native placement samples. The exact installed candidate remained at `(619,2160)-(3179,2880)` on XENEON EDGE; live clicks on Modes, Quick controls, and Reset local data had no effect while locked, the lock survived reload, Unlock Touch removed all four inert regions and returned focus to Touch Lock, and post-unlock Modes navigation worked. The transparent guard added no opaque overlay, the reset endpoint was never called, and the runtime tail contained only successful INFO requests. |
| BETA-053 | Recovery and Display Controls remained on `Checking` indefinitely until each panel was opened, even though their rail badges imply background readiness. A first attempted fix also proved that querying DDC/CI during every dashboard boot is too expensive. | High / feature-status truth and startup performance | Exact installed revision-49 audit: Recovery still read `Checking` after multiple seconds and changed to `Ready` immediately only after selection; Display Controls likewise stayed `Checking`. The initial background DDC/CI probe pushed the rendered cold-boot suite past its four-minute bound. | Product/UI + Native display + Recovery + Release/QA | Resolve Recovery through one lightweight post-hydration GET without activation; expose Display Controls as terminal `Available` until opened; keep real DDC/CI qualification inside its panel; verify both badges while other panels remain selected and retain the 100-cold-boot bound | Closed locally | Revision `20260721-50` complete check passed in 163.6 seconds with 152/152 native tests, seven geometries, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect coverage, and 100,990 native placement samples. In the exact installed candidate, Display Controls showed `Available` while System Monitor remained selected and Recovery showed `Ready` while Diagnostics remained selected. A rejected non-local Frigate address preserved the typed address/filter, left Camera Detection unconfigured, and produced no dashboard failure. |
| BETA-054 | Diagnostics stretched the small UniFi integration card to the full height of the Camera Detection form, producing a large opaque empty block beside Camera setup; both rendered validators also treated Edge's launcher-process handoff as browser exit and could leave locked isolated profiles. | High / UI clarity and release-test reliability | Exact installed revision-50 Camera setup handoff measured the UniFi card at 544 px—the same height as the Camera form—despite containing only 110 px of content. The first revision-51 rendered attempt served the unrecompiled revision-50 stylesheet, and later Edge runs proved the debugging endpoint remained healthy after the original launcher handle exited. | Product/UI + Camera Detection + Release/QA | Top-align optional integration cards so short cards retain natural height; assert a material UniFi/Camera height difference after the real Camera handoff; follow Edge's debugging endpoint across process handoff; close the real browser through CDP; rerun the complete integrated gate, package, install, and measure the exact candidate | Closed locally | Revision `20260721-51` complete check passed in 652.1 seconds with 152/152 native tests, seven rendered geometries, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 28,965 native placement samples, and zero build warnings/errors. The installed UI serves revision 51, focuses the Camera address, reports `align-items: start`, measures Camera at 544 px and UniFi at 110 px, has no horizontal overflow or console warning/error, and the 590-line installed-session snapshot at final audit contains zero warnings, errors, or reset calls. |
| BETA-055 | All eight shipped standalone fallback pages contained inline runtimes that the native Content Security Policy did not nonce, so direct/fallback execution could be blocked; installed Diagnostics reproduced as an inert `Loading` surface with obsolete `Auto setup & diagnostics` and disabled `Auto ready` UI. | High / fallback reliability and UI consistency | Installed revision-51 direct `/widgets/setup-guide.html` returned HTTP 200 and loaded `widget-core.js`, but its runtime never requested health/config/Hue and the DOM remained at its initial Loading placeholder. Source inspection showed the response CSP allowed only self plus the bootstrap nonce while the page's own inline script had no nonce. | Native host + Product/UI + Release/QA | Apply the response nonce to every inline script in native-served HTML while preserving the strict CSP; verify every shipped fallback's CSP/tag agreement; render standalone Diagnostics through a real browser; align its naming and completion action with the main UI; package, install, and inspect the exact candidate | Closed locally | Revision `20260721-52` complete check passed in 145.1 seconds with 152/152 native tests, seven rendered geometries, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, eight standalone CSP checks, direct rendered Diagnostics terminal-state proof, 162,399 native placement samples, and zero build warnings/errors. The exact installer exited 0; installed revision 52 serves two inline Diagnostics scripts with the published nonce, uses the `Diagnostics` heading, contains no `Auto ready`, matches published EXE/DLL hashes, remains on XENEON EDGE at 2560×720/60 Hz, and its 73-line installed-session snapshot contained zero warnings, errors, or reset calls. |
| BETA-056 | Shipped standalone fallbacks still disagreed with the main product: Calendar displayed four fabricated meetings, Network used a browser estimate, System Monitor claimed a bridge was required, Audio could expose a raw device GUID, Diagnostics exposed self-referential developer embed controls, Hue Refresh was below the touch minimum, and three pages requested CSP-blocked remote fonts. | High / fallback truth, resolution, and touch usability | Installed revision-52 audit of all eight direct fallback pages at the XENEON EDGE's 2560×720 viewport plus source tracing of each default endpoint and render path | Product/UI + Native host + Accessibility + Release/QA | Use native local endpoints by default; render truthful configured/unconfigured states; remove fake/developer/private copy and remote fonts; enforce 44px actions; render and inspect all eight fallbacks at 2560×720 with no loading loop, overflow, fatal state, or CSP failure | Closed locally | Revision `20260721-53` complete check passed in 145.8 seconds with 152/152 native tests, seven geometries, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, all eight fallback terminal/CSP/2560×720 checks, and 158,978 native placement samples. Exact installer SHA256 `EC746EA2AF6642AE04D70B82537C3990EE5AF2B3F671FD8838DCEBEF4B7ED8B3` was installed in place; installed revision and EXE/DLL hashes match the build, browser geometry is exactly 2560×720 with no overflow or undersized controls, direct physical capture is 2560×720 with no visible taskbar, and the 382-line installed session contains zero warnings, errors, or reset calls. |
| BETA-057 | The installed 2560×720 product-surface sweep found undersized Layout Editor controls and supporting text below the 12-pixel readability floor in System Monitor, Audio & Media, and Network. | High / companion-display resolution, touch, and readability | Installed revision-53 audit of all 19 visible registered product surfaces at the XENEON EDGE's native viewport: Layout Editor `Up` buttons measured 39.3 px wide and `Pin` measured 40.4 px; supporting labels measured 10.88-11.84 px | Product/UI + Accessibility + Release/QA | Enforce a 44×44 minimum for every Layout Editor inline action; raise the verified supporting labels to at least 12 px; make the rendered suite open and audit every visible registered surface at exact 2560×720 for terminal state, touch size, text size, names, clipping, overflow, duplicate titles, and fatal/runtime errors; rebuild, install, and physically inspect the exact candidate | Closed locally | Revision `20260721-54` complete check passed in 167.6 seconds with 152/152 native tests, seven main geometries, all 19 visible registered product surfaces plus eight standalone fallbacks at 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 160,547 native placement samples, and zero build warnings/errors. The exact installer SHA256 `ED01DF32DA84B3544855FD801F0BB70F918676005654270D03CC6007CA15D93B` was installed in place; installed Layout Editor has 57 visible inline controls with a 44×44 minimum, verified System/Audio/Network labels have a 12 px minimum, viewport/body/document are exactly 2560×720 with no overflow or fatal state, browser logs are empty, and a direct physical capture shows edge-to-edge XENEON EDGE coverage with no taskbar or error overlay. |
| BETA-058 | Four shipped feature surfaces were excluded from the complete UI sweep. Calendar, Weather, and Hue could not be opened until configured, while Clipboard History was unconditionally hidden even though the native host advertised it as available and implemented a privacy-safe panel. | High / feature discoverability, privacy, and release coverage | Installed revision-54 deep links for `clipboard`, `weather`, `calendar`, and `hue` all fell back to Network. Source tracing found `return false` for Clipboard History and configuration-gated visibility for the other three. | Product/UI + Privacy + Accessibility + Release/QA | Keep all supported optional surfaces discoverable in Apps & Controls; never enumerate clipboard contents during health checks; hide previews by default; provide direct setup handoffs; keep unconfigured integrations out of normal Home Mode layouts and unready Hue controls out of Quick controls; include every registered surface in the exact 2560×720 sweep and Clipboard History in Layout Editor | Closed locally | Revision `20260721-55` complete check passed in 168.7 seconds with 152/152 native tests, seven geometries, all 23 registered product surfaces plus eight standalone fallbacks at exact 2560×720, direct Weather/Calendar setup handoffs, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 161,091 native placement samples, and zero build warnings/errors. Exact installer SHA256 `BFD254B22F5A74A21B804131C7C102FA48E9EEB24D7BB2E98C15CDFED3875F8D` installed successfully. Installed Clipboard History rendered 12 entries only as `Hidden by privacy mode`; Weather and Calendar opened and focused their intended Diagnostics fields; Hue exposed its setup form while the unlinked Lights shortcut remained hidden; Layout Editor listed 12 panels with 62 controls at a 44×44 minimum; Home contained only its four configured surfaces; browser logs were empty; and the physical capture remained taskbar-free at 2560×720. |
| BETA-059 | Background telemetry polling could be mistaken for the previously reported whole-dashboard refresh, and the release suite did not directly prove that a changing Home metric preserves the mounted shell, cards, controls, focus, and scroll position. | High / runtime stability and perceived flicker | Installed revision-55 observation across multiple polling cycles: Diagnostics remained byte-for-byte stable; Home CPU changed while the same DOM nodes remained mounted. Source cadence is 8 seconds for game/media in Balanced mode and 30 seconds for bridge/launcher refresh. | Product/UI + Release/QA | Observe the installed build through sustained polling with no loading/fatal state; prove stable Diagnostics produces no DOM change; prove Home telemetry changes in place without replacing controls or stealing focus; add an automated regression test | Closed locally | Installed Diagnostics had zero panel or shell changes, zero loading samples, and zero fatal samples over 40 seconds. Installed Home changed CPU from 22% to 23% while preserving the root, mount, shell, all four metric cards, every button, and scroll position. The new isolated rendered guard performs two real Home API refreshes and passed with mount/card/control identity and keyboard focus preserved. Complete `npm run check` passed in 165.5 seconds with 152/152 native tests, all 23 product surfaces at exact 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection loss/reconnect, 160,914 native placement samples, and zero build warnings/errors. Auxora was restarted afterward and returned healthy on revision `20260721-55`, targeting the non-primary XENEON EDGE at `(619,2160)` and 2560×720. |
| BETA-060 | Windows forced-colors/High Contrast behavior was not rendered by the release suite. The untested UI kept decorative glow layers, removed visible boundaries from Home actions, Game Mode launch cards, and Camera Detection setup/refresh buttons, and allowed Theme Studio's color input to opt out of the system palette. | High / accessibility, input visibility, and companion-display certification | New three-theme and all-surface CDP forced-colors probes failed successively on the ambient canvas/layer, `.inline-button`, `.game-mode-steam-launch`, `.router-settings__button`, and `.product-color-input` before the fixes were accepted. | Product/UI + Accessibility + Release/QA | At exact 2560×720, render every product surface with `forced-colors: active`; require decorative layers hidden, every visible button bounded, selected navigation distinct, keyboard focus outlined, no forced-color opt-outs, no overflow/fatal state, and unchanged reduced-motion coverage | Closed locally | Revision `20260722-56` complete `npm run check` passed in 178.5 seconds with 152/152 native tests, seven geometries, all 23 surfaces in both the normal and Windows forced-colors sweeps at exact 2560×720, three-theme forced-colors selection/focus checks, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 162,557 native placement samples, and zero build warnings/errors. Exact installer SHA256 `DEBE288E564F4F8CD457D57FB0370E8C83336773F804907744F0907B7B0A7E8A` installed successfully despite its wrapper exceeding the five-minute wait; no installer process remained, the install transcript closed normally, installed health/assets report revision 56, published and installed EXE/DLL hashes match, served CSS contains every forced-colors rule, and the installed session contains zero warnings/errors. |

| BETA-061 | Manifest and exact-candidate receipt validators allowed PowerShell JSON coercion to treat strings, numbers, or arrays as required Boolean/integer evidence and accepted loosely formatted or stale completion timestamps. Public docs also omitted the x64/WebView2 requirements and contradicted the intentional release-page-only unsigned update flow. | Critical / publication evidence integrity | Independent current-HEAD audit of `Test-BetaLifecycleReceipt.ps1`, `Test-FrigateQualificationReceipt.ps1`, `Test-DisplayQualificationReceipt.ps1`, and release docs | Release/QA | Preserve JSON token types on Windows PowerShell 5.1 and PowerShell 7; require exact object shapes, names, strings, integers, Booleans, UTC timestamps, freshness, and exact check sets; exercise adversarial fixtures under both engines; state truthful requirements and unsigned update discovery | Closed in source | Dual-engine lifecycle, Frigate, display, and manifest suites accept valid fixtures and reject string/Boolean/fractional schema values, coerced check evidence, wrong-case/extra properties, unsafe numeric fields, and stale/future/non-UTC timestamps. Exact workflow candidate and physical receipts remain pending. |
| BETA-062 | The publication job named `beta-publication`, but the live repository had no such environment, so GitHub could create it without reviewers or approval protections when first referenced. | Critical / publication authorization | Live `GET /repos/SilverFuel/xeneon-widgets/environments/beta-publication` returned 404 on 2026-08-18; workflow referenced the name without checking its configuration | Release/QA + repository owner | A preflight job verifies the environment exists before the publish job references it, requires one to six named reviewers and exact Boolean `prevent_self_review=true`, repeats that check immediately before upload, and dual-engine fixtures reject absent/coerced protections | Closed in automation; GitHub configuration pending | Source and fixtures fail closed. The repository owner must still create and protect the live environment, including manually disabling administrator bypass, before publication can run. |

## Baseline validation receipts

| Command | Result |
| --- | --- |
| `npm run check` | Pass: all repository checks, 20/20 .NET tests, zero build warnings/errors |
| `npm run audit:deps` | Pass: npm root, Electron npm, and NuGet reported zero known vulnerabilities |
| `git diff --check` | Pass with two line-ending conversion warnings; no whitespace errors |

## Integrated validation receipts

| Command | Result |
| --- | --- |
| `npm run check` | Pass: all repository checks, 142/142 .NET tests, seven rendered geometries, 15 theme captures, 100/100 cold boots, tracked-dashboard startup recovery with embedded-page proof, widget-generation status/error ownership and selector click stability, identical-HTML zero-mutation coverage, quiet background Home refresh, Windows-route-aware Network telemetry and dark-custom-accent contrast, eight-second destructive-action expiry, truthful Home/Mode/display/manual-recovery states, stable reordered multi-control drafts, serialized refresh, stale-mount isolation, unique Panel options and private Audio accessible names, process-identity-free System telemetry with a Task Manager handoff, quiet unconfigured Camera automation with immediate manual refresh, shared concurrent Camera authentication renewal, local setup feedback with duplicate-submit cancellation, complete unconfigured/authenticated/connection-loss/reconnect Camera Detection rendering, exact standalone-fallback asset allowlisting, portable backup and media-privacy interactions, 132,652 native placement samples, live companion-only DDC/CI/native-host/bridge APIs, and zero build warnings/errors |
| `npm run check` (revision `20260720-39`) | Pass: 147/147 .NET tests, seven rendered geometries, 15 Theme captures, 100/100 cold boots, no clipped primary labels, one Theme selector, complete custom Mode save/delete/fallback behavior, fail-closed launched-host profile isolation, Camera in-memory state purge with stale-response protection, non-blocking telemetry startup, clipboard-free health status, truthful companion-absent Recovery, 3,650 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260720-40`) | Pass: 148/148 .NET tests, seven rendered geometries, 15 Theme captures, focused settings change/blur reconciliation, non-touch scroll first-click activation, concise truthful setup chrome with no redundant rail card, one unconfigured Camera setup state, authenticated Camera loss/cache/reconnect behavior, nonblocking cancellation of in-flight old credentials, 100/100 cold boots, 3,129 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260720-41`) | Pass: 148/148 .NET tests, seven rendered geometries, 15 Theme captures, 100/100 cold boots, complete shipped-renderer coverage including Camera Detection, truthful unavailable-panel recovery, authenticated Camera loss/cache/reconnect behavior, 3,995 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260720-42`) | Pass: 149/149 .NET tests, seven rendered geometries, 15 Theme captures, focused Theme Studio change/blur and Custom accent behavior, stable Diagnostics naming, provisioning API minimization, authenticated Camera loss/cache/reconnect behavior, 100/100 cold boots, 3,422 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260720-43`) | Pass in 652 seconds: 149/149 .NET tests, seven rendered geometries, 15 Theme captures, focused Theme Studio change/blur and Custom accent behavior, explicit session-token coverage for background mutations, authenticated Camera loss/cache/reconnect behavior, 100/100 cold boots, 4,000 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260720-44`) | Pass in 609.2 seconds: 149/149 .NET tests, seven rendered geometries, 15 Theme captures, truthful actionable Diagnostics completion state, authenticated Camera loss/cache/reconnect behavior, 100/100 cold boots, 3,401 native placement samples with no visible primary-display window, and zero build warnings/errors |
| `npm run check` (revision `20260721-46`) | Pass in 153 seconds: 151/151 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, truthful healthy Diagnostics visibility, stable scroll/click identity, 100/100 cold boots, 173,885 live HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-47`) | Pass in 192.1 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect coverage, stable XENEON hardware-label tests, 78,858 live HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-48`) | Pass in 200.8 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect coverage, expiring Diagnostics reset confirmation with warning status ownership and zero destructive calls, 75,853 live HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-49`) | Pass in 204.8 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, installed Touch Lock isolation/persistence/focus recovery, deterministic visible reset-confirmation cancellation during an overlapping refresh, Camera Detection live/loss/reconnect coverage, 80,991 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-50`) | Pass in 163.6 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, unopened Recovery and Display Controls terminal-status coverage, Camera Detection live/loss/reconnect coverage, 100,990 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-51`) | Pass in 652.1 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, natural-height Diagnostics integration-card coverage after the Camera setup handoff, Edge process-handoff cleanup, Camera Detection live/loss/reconnect coverage, 28,965 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-52`) | Pass in 145.1 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, CSP nonce coverage for all eight shipped standalone fallbacks, direct rendered Diagnostics execution and terminal-state proof, 162,399 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-53`) | Pass in 145.8 seconds: 152/152 .NET tests, seven rendered geometries including 2560×720, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, eight standalone fallback terminal-state/CSP/geometry/product-truth checks at 2560×720, 158,978 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-54`) | Pass in 167.6 seconds: 152/152 .NET tests, seven rendered geometries, all 19 visible registered product surfaces plus eight standalone fallbacks at exact 2560×720 with terminal-state/touch/text/naming/clipping/overflow/runtime assertions, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 160,547 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260721-55`) | Pass in 168.7 seconds: 152/152 .NET tests, seven rendered geometries, all 23 registered product surfaces plus eight standalone fallbacks at exact 2560×720, privacy-safe Clipboard History, Weather/Calendar setup handoffs, configured-only Mode inclusion, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 161,091 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run check` (revision `20260722-56`) | Pass in 178.5 seconds: 152/152 .NET tests, seven rendered geometries, all 23 product surfaces in normal and Windows forced-colors sweeps at exact 2560×720, system-color control boundaries/selection/focus, reduced motion, 15 Theme captures, 100/100 cold boots, Camera Detection live/loss/reconnect, 162,557 native HWND placement samples on the two-display topology, and zero build warnings/errors |
| `npm run test:frigate-receipt` | Pass: exact-candidate physical receipt fixture accepted; secret-bearing, fixture-only, plaintext-authenticated, stale-event, and failed-reconnect receipts rejected |
| `npm run test:display-receipt` | Pass: physical display fixture accepted; non-physical, primary-intersection, visible-taskbar, incomplete-scaling, failed-touch, and private-path receipts rejected |
| Focused Product/Recovery/Calendar tests after independent review fixes | Pass: 24/24, including in-flight foreground capture versus opt-out |
| `npm run test:rendered-dashboard` | Pass: compact 1280x400, standard 1440x900, ultrawide 2560x720, portrait 800x1280, plus initialized WebView reset |
| `npm run test:native-host-api` | Pass: live health, config, embedded assets, origin rejection, and mutation-token rejection |
| `npm run audit:deps` | Pass: root npm, Electron npm, and NuGet reported zero known vulnerabilities |
| `git diff --check` | Pass: no whitespace errors |
| Exact-candidate artifact and manifest verification | Pending: no authoritative workflow candidate for the current commit exists yet; version, commit SHA, filename, SHA-256 sidecar, exact `NotSigned` status, notes, and five-file allowlist must still be verified together |
| Exact-candidate qualification receipts | Pending: no schema-3 disposable-VM lifecycle receipt, physical Frigate/camera receipt, or physical companion-display receipt exists for the current candidate |
| Non-installing release gauntlet | Pending for the authoritative workflow candidate; historical/local gauntlet results do not qualify the current release artifact |

## Operational stop rules

- Do not push, tag, sign, publish, or replace release assets without explicit user approval.
- Do not install or uninstall on the user's normal Windows profile.
- Do not mark BETA-011 complete from source tests or a locally built but uninstalled artifact.
